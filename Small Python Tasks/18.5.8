"""Проверьте, все ли элементы в списке являются уникальными."""

# мой вариант

A = [1, 4, 5, 6, 7, 9, ]
A = sorted(A)
A_set = sorted(set(A))

print(A, A_set)
print(A == A_set)

# вариант с платформы (ЛУЧШЕ)

list_ = [-5, 2, 4, 8, 12, -7, 5]

print(len(list_) == len(set(list_)))
