# Решение:

# Процентные ставки банков
per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

# На вход программы с клавиатуры вводится сумма money, которую человек планирует положить под проценты
money = int(input('Введите сумму в рублях, которую хотите положить под проценты'))

# Формирование списка накопленных средства за год вклада в каждом из банков
deposit = list((round(money * per_cent['ТКБ'], 2),
                round(money * per_cent['СКБ'], 2),
                round(money * per_cent['ВТБ'], 2),
                round(money * per_cent['СБЕР']), 2))

# Поиск масимального значения среди элементов списка
max_deposit = max(deposit)

print('Максимальная сумма, которую вы можете заработать — ', max_deposit, 'руб.')
